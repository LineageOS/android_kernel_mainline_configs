# Options that are still missing:
# - CONFIG_CPU_FREQ_TIMES
# - CONFIG_DM_DEFAULT_KEY
# - CONFIG_NETFILTER_XT_MATCH_QUOTA2
# - CONFIG_NETFILTER_XT_MATCH_QUOTA2_LOG
# - CONFIG_SCHED_DEBUG
# - CONFIG_UID_SYS_STATS

# Default of CONFIG_LSM when CONFIG_DEFAULT_SECURITY_SELINUX == y (required for Android to boot)
CONFIG_LSM="landlock,lockdown,yama,loadpin,safesetid,selinux,smack,tomoyo,apparmor,ipe,bpf"

# Dependency of CONFIG_HID_PLAYSTATION == y
CONFIG_LEDS_CLASS_MULTICOLOR=y

# Dependency of CONFIG_IFB == y
CONFIG_NET_ACT_MIRRED=y
